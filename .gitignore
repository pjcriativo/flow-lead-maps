node_modules
dist

# Segredos — NUNCA versionar (.env e variantes, exceto o template)
.env
.env.*
!.env.example

# Artefatos de build (TanStack Start / Nitro / Cloudflare / Vite)
.output
.wrangler
.nitro
.vite
.tanstack

# Arquivos sensíveis / não versionar
contas.txt
*.zip
