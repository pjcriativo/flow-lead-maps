#!/usr/bin/env node
// PROVA — UX do pool Apify: (1) add com formato inválido (e-mail) é RECUSADO com motivo;
// (2) add válido TESTA automaticamente e devolve o resultado na hora; (3) o resultado do
// teste PERSISTE (teste_ok/teste_detalhe/testada_em aparecem no listar — nada de campo mudo);
// (4) "Simular esgotamento" marca a chave e responde QUEM assume (rodízio demonstrado sem
// gastar); (5) listar traz a ÚLTIMA BUSCA com a chave que pagou; (6) token nunca em texto
// pu
